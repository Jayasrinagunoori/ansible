- name: install and start nginx
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      start: present
      
  - name: start nginx
    service:
      name: nginx
      start: present
      enabled: yes
